package com.bhoomidrishti.common;

/**
 * Shared building blocks that are used by more than one module live here (for example API response
 * envelopes, pagination helpers and constants).
 *
 * <p>Nothing is shared yet in Phase 1, so the package is intentionally empty.
 */
