package com.bhoomidrishti.repository;

/**
 * Spring Data repositories live here.
 *
 * <p>Phase 1 intentionally contains no repository: database entities are introduced together with
 * the domain modules of the next phases. The package exists so the layout is stable and new
 * repositories have an obvious place.
 */
