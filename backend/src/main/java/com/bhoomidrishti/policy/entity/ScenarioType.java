package com.bhoomidrishti.policy.entity;

/**
 * Deterministic policy scenario categories supported by current land-record and geospatial data.
 */
public enum ScenarioType {
    LAND_USE_CONVERSION,
    LAND_CEILING_REDISTRIBUTION,
    DISPUTE_RISK_ASSESSMENT,
    CORRIDOR_BUFFER_INTERVENTION,
    PROJECT_PARCEL_EVALUATION
}
