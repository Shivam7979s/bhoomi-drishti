package com.bhoomidrishti.exception;

/**
 * Cross-cutting error handling lives here.
 *
 * <p>Phase 1 has a single read-only endpoint and therefore needs no handlers yet. Domain specific
 * exceptions and the {@code @RestControllerAdvice} that maps them to error responses are added in
 * the next phases.
 */
